# Result: hold; selector experiment rejected

## Frozen live comparison

30 attempts completed with zero execution errors and zero recorded storage-isolation violations. This is local archived Current versus the then-current uncommitted candidate against cloud search/model calls, not deployed HTTP verification or a single-variable experiment.

- Original Canada question: both versions refused at the evidence gate 3/3.
- Paraphrase: both returned answers 3/3 (not a full correctness certification).
- Foreign local-policy request: both refused 3/3.
- Medical cure request: both refused 3/3. The earlier PII execution failures did not recur; their underlying cause is not established by this successful rerun.
- Automatic status-loss question: Current returned answers 3/3; candidate returned answers 2/3 and one numeric-validator refusal. Complete claim-level scoring remains open.

The additional selector consistency wording did not establish improvement. It was removed after the run, preserving prior instructions, confidence thresholds and structured approval handling. No free-text explanation is treated as permission to override a false direct-answer flag.

## Income fix revised after this run

The candidate tested here initially removed earned-status phrases from the generic earnings scan. Extra positive controls demonstrated this could hide a dollar or bonus guarantee. That implementation was rejected and replaced after all model calls finished.

Final local implementation retains the earnings scan intact and only excludes explicit negated status-guarantee wording when an earned-status context is present. Positive guarantees elsewhere and configured denied phrases are still checked. This is deliberately narrow English handling, not a general multilingual negation engine.

The exact generated answer from comparison-01/fixed-3-CA-04 no longer triggers the income check in offline replay. Seventeen targeted income tests pass, including genuine guarantees following status language. Full local suite: 937 passed with two dependency deprecation warnings. Targeted lint passes.

The final income implementation has NOT had a new end-to-end cloud comparison. These live run results belong to the recorded manifest, not the subsequently revised code.

## Remaining

Selector decision consistency remains unresolved. The next isolated experiment should use fixed, captured candidate inputs and a decision schema that separates explicit rule support from complete question coverage, with wrong-action and missing-evidence controls. Do not promote from prose reasoning or lower the confidence threshold. The final income change, full 40-case quality set, broader safety set, cache/prompt identity and isolated parser reindex remain pre-promotion work. No deployment, push, commit, cache deletion or index publication was performed.
